put:
  summary: Send shift exchange
  parameters:
    - name: shiftExchangeId
      in: path
      required: true
      schema:
        type: string
        format: ObjectId
  tags:
    - Shift Exchanges
  responses:
    200:
      description: Sent shift exchange
    400:
      description: |
        * Validation failed
        * Not OPEN
    401:
      description: Unauthorized
    404:
      description: Exchange shift not found
